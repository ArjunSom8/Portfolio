{
  "id": null,
  "title": "Service App Overview",
  "schemaVersion": 16,
  "version": 0,
  "panels": [
    {
      "type": "graph",                
      "title": "HTTP Requests/sec",
      "targets": [
        {
          "expr": "sum(rate(http_requests_total[5m])) by (instance)",
          "legendFormat": "{{instance}}"
        }
      ]
    }
  ]
}